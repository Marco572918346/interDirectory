import { PipeUserFilterPipe } from './pipe-user-filter.pipe';

describe('PipeUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
